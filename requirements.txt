# Core dependency
numpy>=1.20.0

# GPU acceleration (install as needed)
torch>=2.0.0  # For CUDA/MPS GPU support (install separately with appropriate CUDA version)
jax>=0.4.0  # For GPU/TPU acceleration (install separately with CUDA support if using GPU)
jaxlib>=0.4.0  # JAX library dependency

# For installation with GPU support:
# PyTorch with CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu118
# JAX with CUDA: pip install jax[cuda11_local] -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html

